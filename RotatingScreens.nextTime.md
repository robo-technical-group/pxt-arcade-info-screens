# RotatingScreens.nextTime property

Get the next `game.runtime()` when screens should be rotated. Call [`RotatingScreens.rotate`](RotatingScreens.rotate.md) to do so.

```typescript
RotatingScreens.rotate: number
```