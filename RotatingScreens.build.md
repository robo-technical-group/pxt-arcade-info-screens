# RotatingScreens.build method

Initialize the canvas and create sprites. Call when ready to present rotating screens after object has been configured.

```typescript
RotatingScreens.build(): void
```
