# RotatingScreens.destroySprites method

Destroy all moving and static sprites from the game. Call when removing the splash screen from the game.

```typescript
RotatingScreens.destroySprites(): void
```