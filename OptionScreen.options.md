# OptionScreens.options property

Get the option strings. Options appears below the [headlines](RotatingScreens.headlines.md). Each array of strings will appear in its own column.

```typescript
OptionScreens.options: string[][]
```